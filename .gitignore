__pycache__/
*.pyc
.pytest_cache/
.DS_Store
Thumbs.db
desktop.ini
*.log
.venv/
venv/
.idea/
.vscode/

# self-evolve run artifacts (never commit sandbox/run state)
.sie/

# any local secrets that ever leak into the skill repo (belt-and-suspenders; secrets belong in the
# Mode-B companion config repo, not here)
secrets/
*.env
*.pseudo-map
pseudonym_hmac_salt

# real-run corpus/pool DATA lives in the PRIVATE config repo, never here
skills/demand-mining/pool/
# ad-hoc corpus/dump artifacts from a manual `pull_discord.py --out ...` land in scripts/;
# the default (stdout) is safe, but a relative --out writes here. Both are DATA, never committed.
skills/demand-mining/scripts/corpus*.json
skills/demand-mining/scripts/dump_*.txt
