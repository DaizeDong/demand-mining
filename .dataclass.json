{
  "_comment": [
    "Skill Repo Spec v1 s9 -- every path in this repo belongs to exactly one class.",
    "TOOL    = code, SKILL.md, docs, reference data, and metrics ABOUT THE SKILL. Public.",
    "FIXTURE = LIFELIKE test data: rows that look like real records, whose real version is",
    "          sitting right there in the operator's life and is structurally indistinguishable.",
    "          Must be produced by tools/make_fixtures.py -- a real record cannot be regenerated,",
    "          so pasting one in fails at commit. (A self-evidently fake canary or an Acme mock is",
    "          NOT lifelike and does not need a generator; the risk it guards against is a person",
    "          reaching for a convenient REAL example, and there is no temptation to reach for a",
    "          real value that would look obviously wrong next to FAKE_CANARY_XYZ.)",
    "DATA    = anything a REAL RUN produced. Never git-tracked. Lives in the private companion",
    "          config, resolved at runtime by tools/datadir.py. This repo ships only the schema.",
    "data_sealed = a path that HELD real data, was purged, and must stay dead.",
    "",
    "Enforced by tools/data_boundary.py in .githooks and in CI."
  ],
  "_audited": "2026-07-20 (was 2026-07-13) -- re-swept. Programmatic writers still all resolve OUTSIDE the repo. BUT a fleet audit found three untracked real-run corpus artifacts in scripts/ from a manual `pull_discord.py --out corpus_today.json` (default output is stdout and safe; a relative --out lands in cwd=scripts/). They were undeclared and un-gitignored, so both structural gates were blind (pii_guard: redaction stripped every smellable token; data_boundary: undeclared path fires zero checks). Moved out to ~/.demand-mining-config/data/manual-corpus/, gitignored (scripts/corpus*.json + dump_*.txt), and the canonical output name sealed below so the gate hard-fails on any future `git add -f`. An empty data[] is still a CONCLUSION: this skill's programmatic writers write outside the repo; only manual --out could reintroduce DATA, now blocked on two layers.",
  "data": [],
  "data_sealed": [
    "skills/demand-mining/scripts/corpus_today.json",
    "skills/demand-mining/scripts/dump_general.txt",
    "skills/demand-mining/scripts/dump_lorestage-support.txt"
  ],
  "fixture": [],
  "_data_home": "~/.demand-mining-config/data/   (override with $DEMAND_MINING_DATA_DIR). If this skill ever needs to record real-run output, it goes THERE -- never back into this repo."
}
